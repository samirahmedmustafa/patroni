# .bash_profile

# Get the aliases and functions
if [ -f ~/.bashrc ]; then
        . ~/.bashrc
fi

# User specific environment and startup programs

PATH=/product/postgresql/ver/bin:/usr/local/bin/:$PATH
LD_LIBRARY_PATH=/product/postgresql/ver/lib/
export LD_LIBRARY_PATH PATH

